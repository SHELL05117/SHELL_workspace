---
title: Archive
---

# Archive

Archive 当前只保留一件事：`source/` 中的项目媒体资产暂时不能公开，也不能上传到 Git。已确认可公开的图片应移动到 `static/img/source-assets/` 后再被网站调用。

## 当前状态

- `source/` 中的剩余信息暂不公开。
- `source/` 不上传到 Git。
- OSS/CDN 连接暂未申请完成。
- 国内域名 `shellworkspace.cn` 暂不连接。
- 等 ICP 备案许可完成后，再处理 OSS/CDN、国内域名和媒体发布。

## 处理原则

在 ICP 备案许可完成之前，不移动、不发布、不展示 `source/` 中的内容。
